% =============================================================
\section{Background and related work}\label{sec:background}
% =============================================================

\subsection{Volume-conductor field and the activating function}

At neural stimulation frequencies, displacement currents in tissue are
negligible and the extracellular potential satisfies Laplace's equation
away from sources, giving the closed-form monopole
$\phi(\mathbf{x}) = I / (4\pi\sigma\,d)$ for a point source in a
homogeneous isotropic conductor of conductivity $\sigma$~%
\cite{10.1093/acprof:oso/9780195058239.001.0001,10.1093/acprof:oso/9780195050387.001.0001}.
Multiple electrodes follow by superposition. We use this model
throughout; the homogeneity and isotropy assumptions are limitations we
return to in \cref{sec:limitations}.

For a uniform straight cable in an extracellular field $\phi(x,t)$,
Rattay~\cite{rattay1986analysis} showed that the driving term for
transmembrane voltage is proportional to $\partial^2 \phi / \partial
x^2$ along the cable; McNeal's earlier compartmental treatment of
myelinated axons~\cite{Mcneal1976AnalysisOA} contains the same idea in
discrete form. On an arbitrary compartmental tree the discrete analogue
is $[G\boldsymbol{\phi}]_j$, where $G$ is the simulator's axial
diffusion operator, assembled internally. \cref{sec:method} shows how
to recover $G\boldsymbol{\phi}$ from Jaxley specifically.

\subsection{Differentiable neuron simulators}

Jaxley~\cite{deistler2025jaxley} is the only published JAX-native
compartmental simulator with HH-style channel kinetics, branched
morphologies, and reverse-mode autodiff through the time integrator,
to our knowledge.
Diffrax~\cite{DBLP:journals/corr/abs-2202-02435} provides
differentiable ODE solvers in JAX, but does not model compartmental
neurons. Brian2~\cite{Stimberg2019} is Python-native and considerably
faster than NEURON, but is not built for automatic differentiation.
